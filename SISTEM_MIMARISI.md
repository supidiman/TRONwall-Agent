# TRONwall-Agent: Sistem Mimarisi ve Modül İlişkileri

## 🏛️ Genel Mimari Şema

```
┌─────────────────────────────────────────────────────────────────┐
│                    TRONwall-Agent Sistemi                       │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │      start_system.py (Koordinatör)      │
        └─────────────────────────────────────────┘
                    │                    │
        ┌───────────┘                    └───────────┐
        ▼                                            ▼
┌───────────────┐                          ┌──────────────────┐
│  WAF Core     │                          │   AI Agent       │
│  (server.py)  │                          │  (decision_      │
│               │                          │   engine.py)     │
└───────────────┘                          └──────────────────┘
        │                                            │
        ├─── middleware.py                          ├─── analyzer.py
        └─── blocker.py                             ├─── llm_client.py
                                                     └─── daily_report.py
                                                              │
                                                              ▼
                                                    ┌──────────────────┐
                                                    │   RAG Memory      │
                                                    │                  │
                                                    ├─── retriever.py  │
                                                    ├─── learner.py    │
                                                    └─── whitelist_    │
                                                         manager.py    │
                                                              │
                                                              ▼
                                                    ┌──────────────────┐
                                                    │   Dashboard      │
                                                    │  (dashboard.py)  │
                                                    └──────────────────┘
```

## 🔄 Veri Akış Diyagramı

```
HTTP İsteği
    │
    ▼
┌─────────────────┐
│  Flask Server   │ ←─── Port 5000
│  (server.py)    │
└─────────────────┘
    │
    ├─── IP Engelleme Kontrolü (blocker.py)
    │    │
    │    ├─── Engellenmiş? → ❌ 403 Forbidden
    │    └─── İzinli? → Devam
    │
    ▼
┌─────────────────┐
│  Middleware     │
│  (middleware.py)│
└─────────────────┘
    │
    ├─── İsteği Parse Et
    ├─── JSON Formatına Çevir
    └─── traffic.log'a Yaz
         │
         ▼
┌─────────────────┐
│ Decision Engine │ ←─── Log Dosyasını İzler (tail -f)
│ (decision_      │
│  engine.py)     │
└─────────────────┘
    │
    ├─── Pre-Filter Kontrolü
    │    │
    │    ├─── Temiz? → ✅ Geçir (AI çağrılmaz)
    │    └─── Şüpheli? → Devam
    │
    ▼
┌─────────────────┐
│   Analyzer      │
│  (analyzer.py)  │
└─────────────────┘
    │
    ├─── [KATMAN 0] Whitelist Kontrolü
    │    │
    │    ├─── Güvenli? → ✅ Geçir
    │    └─── Bilinmiyor? → Devam
    │
    ├─── [KATMAN 1] RAG Hafıza Kontrolü
    │    │
    │    ├─── Eşleşme Var? → ⚡ Hızlı Sonuç (AI çağrılmaz)
    │    └─── Eşleşme Yok? → Devam
    │
    └─── [KATMAN 2] Gemini AI Analizi
         │
         ├─── Saldırı Tespit Edildi?
         │    │
         │    ├─── Evet → Learner'a Gönder
         │    │         │
         │    │         ▼
         │    │    ┌─────────────┐
         │    │    │   Learner   │
         │    │    │ (learner.py)│
         │    │    └─────────────┘
         │    │         │
         │    │         └─── attack_signatures.json'a Ekle
         │    │
         │    └─── Decision Engine'e Sonuç Döndür
         │
         └─── Decision Engine → IP Engelleme
              │
              └─── blocked_ips.json'a Ekle
```

## 📦 Modül Detayları

### 1. WAF Core Modülü

```
waf_core/
├── server.py          → Flask HTTP Sunucusu
│   ├── home()         → Ana endpoint
│   ├── dashboard()    → İzleme paneli
│   └── get_blocked_list() → Engellenen IP'ler
│
├── middleware.py      → İstek İşleme
│   ├── request_parser() → İsteği parse et
│   └── log_transaction() → Log'a yaz
│
└── blocker.py         → IP Engelleme
    └── is_blocked()   → IP kontrolü
```

### 2. AI Agent Modülü

```
ai_agent/
├── analyzer.py        → Ana Analiz Motoru
│   ├── analyze_log()  → 3 katmanlı analiz
│   └── Whitelist → RAG → AI sırası
│
├── decision_engine.py → Karar Mekanizması
│   ├── start_watching() → Log izleme
│   ├── pre_filter()     → Ön filtreleme
│   └── block_ip()       → IP engelleme
│
├── llm_client.py      → Gemini API İstemcisi
│   └── API bağlantısı yönetimi
│
└── daily_report.py    → Günlük Rapor
    └── Executive summary oluşturma
```

### 3. RAG Memory Modülü

```
rag_memory/
├── retriever.py       → Bilgi Tabanı Erişimi
│   └── search_knowledge() → Regex pattern arama
│
├── learner.py         → Otomatik Öğrenme
│   └── learn_new_attack() → Yeni saldırı öğrenme
│
├── whitelist_manager.py → Beyaz Liste
│   ├── add_ip()      → IP ekleme
│   ├── add_path()    → Path ekleme
│   └── is_whitelisted() → Kontrol
│
└── data/
    ├── attack_signatures.json → Saldırı imzaları
    └── whitelist.json         → Güvenli liste
```

## 🎯 Analiz Hiyerarşisi (3 Katmanlı Sistem)

```
                    Log Girdisi
                         │
                         ▼
        ┌────────────────────────────────┐
        │   KATMAN 0: Whitelist Kontrolü│
        └────────────────────────────────┘
                         │
            ┌────────────┴────────────┐
            │                         │
         Güvenli              Bilinmiyor
            │                         │
            ▼                         ▼
        ✅ Geçir              ┌────────────────────┐
                              │ KATMAN 1: RAG      │
                              │ Hafıza Kontrolü    │
                              └────────────────────┘
                                      │
                          ┌───────────┴───────────┐
                          │                       │
                      Eşleşme Var          Eşleşme Yok
                          │                       │
                          ▼                       ▼
                    ⚡ Hızlı Sonuç        ┌──────────────────┐
                    (AI çağrılmaz)       │ KATMAN 2: AI     │
                                          │ (Gemini) Analizi │
                                          └──────────────────┘
                                                  │
                                          ┌───────┴───────┐
                                          │               │
                                      Saldırı         Temiz
                                          │               │
                                          ▼               ▼
                                    🛡️ Engelle      ✅ Geçir
```

## 📊 Veri Dosyaları ve İlişkileri

```
Veri Dosyaları:
├── traffic.log              → Tüm HTTP istekleri
├── blocked_ips.json         → Engellenen IP'ler
├── attack_signatures.json   → Öğrenilmiş saldırı desenleri
└── whitelist.json           → Güvenli IP/Path listesi

İlişkiler:
┌──────────────────┐
│  traffic.log     │ ←─── server.py (middleware.py)
└──────────────────┘
         │
         ▼
┌──────────────────┐
│ decision_engine  │ ←─── Log'u okur ve analiz eder
└──────────────────┘
         │
         ├─── analyzer.py → attack_signatures.json'u okur
         │
         ├─── learner.py → attack_signatures.json'a yazar
         │
         └─── block_ip() → blocked_ips.json'a yazar
              │
              ▼
┌──────────────────┐
│ blocked_ips.json │ ←─── blocker.py tarafından okunur
└──────────────────┘
```

## 🔐 Güvenlik Katmanları

```
┌─────────────────────────────────────────┐
│  KATMAN 1: IP Engelleme (blocker.py)   │
│  - Kara listedeki IP'ler engellenir     │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│  KATMAN 2: Ön Filtreleme                │
│  - Şüpheli karakterler tespit edilir    │
│  - Temiz trafik direkt geçirilir         │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│  KATMAN 3: Whitelist Kontrolü           │
│  - Güvenli IP/Path'ler direkt geçirilir  │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│  KATMAN 4: RAG Hafıza Kontrolü          │
│  - Bilinen saldırılar hızlıca tespit    │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│  KATMAN 5: AI Analizi (Gemini)          │
│  - Bilinmeyen saldırılar tespit edilir  │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│  KATMAN 6: Otomatik Öğrenme             │
│  - Yeni saldırılar hafızaya kaydedilir  │
└─────────────────────────────────────────┘
```

## 🚀 Sistem Başlatma Sırası

```
1. init_db.py
   └─── Klasörler ve JSON dosyaları oluşturulur
   
2. start_system.py
   ├─── waf_core/server.py (Arka plan)
   │    └─── Flask sunucusu başlar (Port 5000)
   │
   └─── ai_agent/decision_engine.py (Arka plan)
        └─── Log izleme başlar
        
3. dashboard.py (Manuel)
   └─── Streamlit dashboard başlar
```

## 📈 Performans Optimizasyonları

```
Optimizasyon Stratejisi:

1. Ön Filtreleme (Pre-Filter)
   └─── %80-90 gereksiz AI çağrısını önler
   
2. Whitelist Kontrolü
   └─── Güvenli trafik için 0ms yanıt
   
3. RAG Hafıza
   └─── Bilinen saldırılar için AI çağrılmaz
   
4. Regex Caching
   └─── Pattern'ler bir kez compile edilir
   
5. Lazy Loading
   └─── Modüller gerektiğinde yüklenir
```

## 🎓 Öğrenme Döngüsü

```
Yeni Saldırı Tespit Edildi
         │
         ▼
┌────────────────────┐
│  Gemini AI Analizi │
└────────────────────┘
         │
         ▼
┌────────────────────┐
│  Saldırı Deseni    │
│  Regex'e Çevrilir  │
└────────────────────┘
         │
         ▼
┌────────────────────┐
│  attack_signatures │
│  .json'a Eklenir   │
└────────────────────┘
         │
         ▼
┌────────────────────┐
│  Gelecekte Aynı    │
│  Saldırı Hızlıca   │
│  Tespit Edilir     │
└────────────────────┘
```

---

**Not:** Bu şema, sistemin genel mimarisini ve modüller arası ilişkileri görselleştirmek için oluşturulmuştur.

